package org.lab11.labwork8;

/**
 * The interface Copy.
 */
public interface Copy {
    /**
     * Method copy() which copy objects.
     *
     * @param depth the depth
     * @return the object
     */
    Catalog copy(int depth);
}
